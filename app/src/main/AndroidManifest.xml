<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.btl_nam3_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OptionGame"></activity>
        <activity android:name=".RuleGame" />
        <activity android:name=".inputHoTen" />
        <activity android:name=".dong_highscore" />
        <activity android:name=".showHighScore" />
        <activity android:name=".about_us" />
        <activity android:name=".FinishPlayGame" />
        <activity android:name=".QuestionInsert" />
        <activity android:name=".dong_question_with_checkbox" />
        <activity android:name=".show_all" />
        <activity android:name=".PlayGame" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>